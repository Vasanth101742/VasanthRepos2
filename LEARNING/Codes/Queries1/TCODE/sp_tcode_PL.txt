--exec sp_tcode_PL
Alter proc sp_tcode_PL
as
Begin

declare @compcount int
declare @count1 int
declare @comp int
declare @lamt_ordr_count int
declare @count2 int

set @compcount=(select count(distinct comp) from tcode_pl)
set @lamt_ordr_count=(select count(distinct lamt_ordr) from tcode_pl)
set @count1=1
set @count2=1


while (@count1<=@compcount)

begin

set @comp=(select comp from(
select comp,row_number() over(order by comp) comprank from 
(select distinct comp from tcode_pl)a)b
where comprank=@count1)

set @count2=1

while (@count2<=@lamt_ordr_count)

begin


------Revenue Calculation--------------------------

--Revenue from operations

update tcode_pl set lamt=a.lamt
from (
select lamt from Tcodeprocess where fcode=1 and slno=21 and subd=1 and ordr=5 
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=1 and subd=1 and ordr=1 
and lamt_ordr=@count2
and comp=@comp


--Less: Excise duty

update tcode_pl set lamt=a.lamt
from (
select  distinct case 
when @count2=1 then b.Amount_in_Reporting_currency1  
when @count2=2 then b.Amount_in_Reporting_currency2  
end as lamt from tcode_pl a
inner join tcode_tb b
on concat(ltrim(rtrim(a.comp)),ltrim(rtrim(a.code)))=b.CombiCode
where a.code<>''
and code='PL103'
)a
where fcode=1 and slno=1 and subd=1 and ordr=2 
and lamt_ordr=@count2
and comp=@comp

--TOTAL

update tcode_pl set lamt=a.lamt
from (
select 
(select lamt from tcode_pl where fcode=1 and slno=1 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
-
(select lamt from tcode_pl where fcode=1 and slno=1 and subd=1 and ordr =2
and lamt_ordr=@count2
and comp=@comp)

as lamt

)a
where fcode=1 and slno=1 and subd=1 and ordr=3 
and lamt_ordr=@count2
and comp=@comp


--Other Income

update tcode_pl set lamt=a.lamt
from (
select lamt from Tcodeprocess where fcode=1 and slno=22 and subd=4 and ordr=8
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=1 and subd=1 and ordr=4 
and lamt_ordr=@count2
and comp=@comp


--Total Revenue

update tcode_pl set lamt=a.lamt
from (
select sum(lamt) lamt from tcode_pl where fcode=1 and slno=1 and subd=1 and ordr in (3,4)
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=1 and subd=1 and ordr=5 
and lamt_ordr=@count2
and comp=@comp

------------IV. Expenses:-----------------------------------------

--Cost of materials consumed

update tcode_pl set lamt=a.lamt
from (
select lamt from Tcodeprocess where fcode=1 and slno=23 and subd=2 and ordr=5
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=2 and subd=1 and ordr=2
and lamt_ordr=@count2
and comp=@comp

--Purchases of Traded goods

update tcode_pl set lamt=a.lamt
from (
select lamt from Tcodeprocess where fcode=1 and slno=24 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=2 and subd=1 and ordr=3
and lamt_ordr=@count2
and comp=@comp

--Changes in Inventories of finished goods work-in-progress and Traded goods

update tcode_pl set lamt=a.lamt
from (
select lamt from Tcodeprocess where fcode=1 and slno=25 and subd=2 and ordr=6
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=2 and subd=1 and ordr=4
and lamt_ordr=@count2
and comp=@comp

--Employee benefits Expense

update tcode_pl set lamt=a.lamt
from (
select lamt from Tcodeprocess where fcode=1 and slno=26 and subd=1 and ordr=17
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=2 and subd=1 and ordr=5
and lamt_ordr=@count2
and comp=@comp

--Selling, administration and others

update tcode_pl set lamt=a.lamt
from (
select lamt from Tcodeprocess where fcode=1 and slno=27 and subd=1 and ordr=6
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=2 and subd=1 and ordr=6
and lamt_ordr=@count2
and comp=@comp

--Finance Costs

update tcode_pl set lamt=a.lamt
from (
select lamt from Tcodeprocess where fcode=1 and slno=28 and subd=1 and ordr=5
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=2 and subd=1 and ordr=7
and lamt_ordr=@count2
and comp=@comp

--Depreciation and Amortisation Expense

update tcode_pl set lamt=a.lamt
from (

select  distinct case 
when @count2=1 then b.Amount_in_Reporting_currency1  
when @count2=2 then b.Amount_in_Reporting_currency2  
end as lamt from tcode_pl a
inner join tcode_tb b
on concat(ltrim(rtrim(a.comp)),ltrim(rtrim(a.code)))=b.CombiCode
where a.code<>''
and code='PL222'
)a
where fcode=1 and slno=2 and subd=1 and ordr=8
and lamt_ordr=@count2
and comp=@comp


--Other Expenses

update tcode_pl set lamt=a.lamt
from (
select lamt from Tcodeprocess where fcode=1 and slno=29 and subd=1 and ordr=55
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=2 and subd=1 and ordr=9
and lamt_ordr=@count2
and comp=@comp

--Total Expenses

update tcode_pl set lamt=a.lamt
from (
select sum(lamt) lamt from tcode_pl where fcode=1 and slno=2 and subd=1 and ordr in (2,3,4,5,6,7,8,9)
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=2 and subd=1 and ordr=10
and lamt_ordr=@count2
and comp=@comp


-----------------------------------------------------------------------------------------------------

--V. Profit Before Exceptional and Extraordinary items and tax (III-IV)

update tcode_pl set lamt=a.lamt
from (
select
(select lamt from tcode_pl where fcode=1 and slno=1 and subd=1 and ordr =5
and lamt_ordr=@count2
and comp=@comp)
-
(select lamt from tcode_pl where fcode=1 and slno=2 and subd=1 and ordr =10
and lamt_ordr=@count2
and comp=@comp)

as lamt

)a
where fcode=1 and slno=3 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp

-----------------------------------------------------------------------------------------------------

--VI.Exceptional Items
update tcode_pl set lamt=a.lamt
from (
select  distinct case 
when @count2=1 then b.Amount_in_Reporting_currency1  
when @count2=2 then b.Amount_in_Reporting_currency2  
end as lamt from tcode_pl a
inner join tcode_tb b
on concat(ltrim(rtrim(a.comp)),ltrim(rtrim(a.code)))=b.CombiCode
where a.code<>''
and code='BS334'
)a
where fcode=1 and slno=4 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp


-----------------------------------------------------------------------------------------------------

--VII. Profit Before Extraordinary items and tax (V-VI)


update tcode_pl set lamt=a.lamt
from (
select
(select lamt from tcode_pl where fcode=1 and slno=3 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
-
(select lamt from tcode_pl where fcode=1 and slno=4 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)

as lamt

)a
where fcode=1 and slno=5 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp

-----------------------------------------------------------------------------------------------------

--VIII. Extraordinary items


update tcode_pl set lamt=a.lamt
from (
select  distinct case 
when @count2=1 then b.Amount_in_Reporting_currency1  
when @count2=2 then b.Amount_in_Reporting_currency2  
end as lamt from tcode_pl a
inner join tcode_tb b
on concat(ltrim(rtrim(a.comp)),ltrim(rtrim(a.code)))=b.CombiCode
where a.code<>''
and code='BS335'
)a
where fcode=1 and slno=6 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp


-----------------------------------------------------------------------------------------------------

--IX. Profit before Tax (VII-VIII)

update tcode_pl set lamt=a.lamt
from (
select
(select lamt from tcode_pl where fcode=1 and slno=5 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
-
(select lamt from tcode_pl where fcode=1 and slno=6 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)

as lamt

)a
where fcode=1 and slno=7 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp

-----------------------------------------------------------------------------------------------------

--X. Tax Expense:

--(1) Current Tax

update tcode_pl set lamt=a.lamt
from (
select  distinct case 
when @count2=1 then b.Amount_in_Reporting_currency1  
when @count2=2 then b.Amount_in_Reporting_currency2  
end as lamt from tcode_pl a
inner join tcode_tb b
on concat(ltrim(rtrim(a.comp)),ltrim(rtrim(a.code)))=b.CombiCode
where a.code<>''
and code='PL185'
)a
where fcode=1 and slno=8 and subd=1 and ordr=2
and lamt_ordr=@count2
and comp=@comp



--(2) Deffered Tax

update tcode_pl set lamt=a.lamt
from (
select  distinct case 
when @count2=1 then b.Amount_in_Reporting_currency1  
when @count2=2 then b.Amount_in_Reporting_currency2  
end as lamt from tcode_pl a
inner join tcode_tb b
on concat(ltrim(rtrim(a.comp)),ltrim(rtrim(a.code)))=b.CombiCode
where a.code<>''
and code='PL186'
)a
where fcode=1 and slno=8 and subd=1 and ordr=3
and lamt_ordr=@count2
and comp=@comp

--Total

update tcode_pl set lamt=a.lamt
from (
select sum(lamt) lamt from tcode_pl where fcode=1 and slno=8 and subd=1 and ordr in (2,3)
and lamt_ordr=@count2
and comp=@comp
)a
where fcode=1 and slno=8 and subd=1 and ordr=4
and lamt_ordr=@count2
and comp=@comp


-----------------------------------------------------------------------------------------------------

--XI. Profit/(Loss) for the period from continuing operations (VII-VIII)

update tcode_pl set lamt=a.lamt
from (
select
(select lamt from tcode_pl where fcode=1 and slno=7 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
-
(select lamt from tcode_pl where fcode=1 and slno=8 and subd=1 and ordr =4
and lamt_ordr=@count2
and comp=@comp)
as lamt
)a
where fcode=1 and slno=9 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp


-----------------------------------------------------------------------------------------------------

--XII. Profit/(loss) from discontinuing operations


update tcode_pl set lamt=a.lamt
from (
select  distinct case 
when @count2=1 then b.Amount_in_Reporting_currency1  
when @count2=2 then b.Amount_in_Reporting_currency2  
end as lamt from tcode_pl a
inner join tcode_tb b
on concat(ltrim(rtrim(a.comp)),ltrim(rtrim(a.code)))=b.CombiCode
where a.code<>''
and code='BS336'
)a
where fcode=1 and slno=10 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp


-----------------------------------------------------------------------------------------------------


--XIII. Tax Expense of discontinuing operations


update tcode_pl set lamt=a.lamt
from (
select  distinct case 
when @count2=1 then b.Amount_in_Reporting_currency1  
when @count2=2 then b.Amount_in_Reporting_currency2  
end as lamt from tcode_pl a
inner join tcode_tb b
on concat(ltrim(rtrim(a.comp)),ltrim(rtrim(a.code)))=b.CombiCode
where a.code<>''
and code='BS337'
)a
where fcode=1 and slno=11 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp


-----------------------------------------------------------------------------------------------------

--XIV. Profit/(loss) from discontinuing operations (after tax) (XII-XIII)

update tcode_pl set lamt=a.lamt
from (
select
(select lamt from tcode_pl where fcode=1 and slno=10 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
-
(select lamt from tcode_pl where fcode=1 and slno=11 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
as lamt
)a
where fcode=1 and slno=12 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp


-----------------------------------------------------------------------------------------------------

--XV. Profit (loss) for the period (XI+XIV)

update tcode_pl set lamt=a.lamt
from (
select
(select lamt from tcode_pl where fcode=1 and slno=9 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
+
(select lamt from tcode_pl where fcode=1 and slno=12 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
as lamt
)a
where fcode=1 and slno=13 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp

-----------------------------------------------------------------------------------------------------


--XVI. Earnings per equity share

update tcode_pl set lamt=1
where fcode=1 and slno=14 and subd=1 and ordr=1
and lamt_ordr=@count2
and comp=@comp


--Nominal value of the shares

--(1) Basic

update tcode_pl set lamt=a.lamt
from (
select
(select lamt from tcode_pl where fcode=1 and slno=13 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
/
(select case when lamt=0 then 0.000001 else lamt end as lamt from Tcodeprocess where fcode=1 and slno=1 and subd=1 and ordr =7
and lamt_ordr=@count2
and comp=@comp)
as lamt
)a
where fcode=1 and slno=14 and subd=1 and ordr=3
and lamt_ordr=@count2
and comp=@comp

--(2) Diluted


update tcode_pl set lamt=a.lamt
from (
select
(select lamt from tcode_pl where fcode=1 and slno=13 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
/
(select case when lamt=0 then 0.000001 else lamt end as lamt from Tcodeprocess where fcode=1 and slno=1 and subd=1 and ordr =7
and lamt_ordr=@count2
and comp=@comp)
as lamt
)a
where fcode=1 and slno=14 and subd=1 and ordr=4
and lamt_ordr=@count2
and comp=@comp


-----------------------------------------------------------------------------------------------------

--XVI. Earnings per Equity Share from continuing operations

--Nominal value of the shares

--(1) Basic

update tcode_pl set lamt=a.lamt
from (
select
(select lamt from tcode_pl where fcode=1 and slno=13 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
/
(select case when lamt=0 then 0.000001 else lamt end as lamt from Tcodeprocess where fcode=1 and slno=1 and subd=1 and ordr =7
and lamt_ordr=@count2
and comp=@comp)
as lamt
)a
where fcode=1 and slno=15 and subd=1 and ordr=3
and lamt_ordr=@count2
and comp=@comp

--(2) Diluted

update tcode_pl set lamt=a.lamt
from (
select
(select lamt from tcode_pl where fcode=1 and slno=13 and subd=1 and ordr =1
and lamt_ordr=@count2
and comp=@comp)
/
(select case when lamt=0 then 0.000001 else lamt end as lamt from Tcodeprocess where fcode=1 and slno=1 and subd=1 and ordr =7
and lamt_ordr=@count2
and comp=@comp)
as lamt
)a
where fcode=1 and slno=15 and subd=1 and ordr=4
and lamt_ordr=@count2
and comp=@comp
-----------------------------------------------------------------------------------------------------
set @count2=@count2+1
end

set @count1=@count1+1
end



end